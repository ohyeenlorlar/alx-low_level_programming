#include "main.h"

/**
*_strlen - a function that prints the length of a string
*@s: char input
*Return: length of the input string
*/
int _strlen(char *s)
{
	int len = 0;
	while (s[len] != '\0')
	{
		len++
	}
	return (len);
}
